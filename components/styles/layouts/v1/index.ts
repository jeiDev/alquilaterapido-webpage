import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    main: {
        minHeight: "calc(100vh - 80px - 95px - 11px)",
        paddingTop: 25
    }
}));

export default useStyles;