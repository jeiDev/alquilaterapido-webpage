import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
    group: {
        width: "100%",
        paddingBottom: 20
    },

    label: {
        fontWeight: "bold",
        fontSize: ".9em",
        marginBottom: ".5rem"
    }
}))

export default useStyles